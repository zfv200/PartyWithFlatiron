<h2>Name of Location: <%= @location.name %></h2><br>

<h2>Name of Events:</h2> <% @events.each do |event| %>
<div class="ui list">
  <div class="item">
    <i class="star icon"></i>
    <div class="content">
    <%= link_to event.title, event_path(event) %>
    </div>
  </div>
</div>



<% end %>
