<h1>All Locations:</h1>
<% @locations.each do |location| %>
<div class="ui list">
  <div class="item">
    <i class="map icon"></i>
    <div class="content">
    <%= link_to location.name, location_path(location) %>
    </div>
  </div>
</div>
<% end %>
