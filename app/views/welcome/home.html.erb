<h1>Welcome, <%= @user.username%></h1>

Events Hosted by Me:<br>
<% @user.events.each do |event| %>
<%= link_to event.title, event_path(event) %><br>
<% end %>
<br>
Events I'm Attending:<br>
<% @user.rsvps.each do |rsvp| %>
  <%= link_to rsvp.event.title, event_path(rsvp.event)%><br>
<% end %>
<br>

My Next Event:<br>
<%= link_to @user.next_event, event_path(Event.find_by(title: @user.next_event)) %>
<br><br><br>
<%= link_to "Add Event", new_event_path%>
<br><br><br><br>

Suggested Events:<br>
<% @user.suggested_events.each do |event| %>
<%= link_to event.title, event_path(event) %><br>
<% end %>
<br><br><br><br>
<%= button_to "Deactivate Profile", "/confirm", method: "get"%>
